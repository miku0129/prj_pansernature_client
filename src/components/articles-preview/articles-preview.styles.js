import { CustomLink } from "../../utilities/components.styles";
import styled from "styled-components";

export const ArticlesPreviewCustomLink = styled(CustomLink)`
  &:hover {
    opacity: 0.8;
  }
`;
